# V1 model

Ignore this directory and it's sub-directories. It is a checkpoint of v1 model code, which will be integrated after careful inspection. For now, it is simply duplicated in the `Plantwise_v1` repo, and all changes should go there instead. 

## Notes 

1. I am sharing the R code for 10 random species, along with their results (a result CSV file). Paths for files need to be changed. 
2. A file named `final_species_output`, which is the input database of all species and locations. Kindly change the name if it is confusing, and similar changes need to be made in the code as well. 
3. Also sharing the Western Ghats boundary shape file if necessary.
4. I am sharing a research paper with methodology (page 5, highlighted text), similar changes to be made in line 64 of the code, which is tune.args wherein more FC and RM need to be added. 
